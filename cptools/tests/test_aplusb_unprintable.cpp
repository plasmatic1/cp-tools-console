#include <bits/stdc++.h>

using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    cout << char(a + b) << endl;
    cout << (unsigned char)(193) << endl; // Courtesy of Ray Kong
    return 0;
}
